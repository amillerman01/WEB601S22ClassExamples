import { FilterChessPlayerTypePipe } from './filter-chess-player-type.pipe';

describe('FilterChessPlayerTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChessPlayerTypePipe();
    expect(pipe).toBeTruthy();
  });
});
